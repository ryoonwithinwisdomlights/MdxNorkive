---
title: '[TIL] 모노레포 패키지 관리 베스트 프랙티스'
slug: techs-TIL-모노레포-패키지-관리-베스트-프랙티스
summary: ''
pageCover: >-
  https://images.unsplash.com/photo-1593062096033-9a26b09da705?ixlib=rb-4.0.3&q=85&fm=jpg&crop=entropy&cs=srgb
notionId: 29e1eb5c03378065bddfdf214e10cbfc
password: ''
type: Techs
doc_type: TIL
category: TIL
tags:
  - 개발TIL
  - 기술로그
favorite: false
date: '2025-11-01'
lastEditedTime: '2026-01-18T17:45:00.000Z'
draft: false
description: ''
icon: ''
full: false
readingTime: 16
wordCount: 3054
status: published
author: ryoonwithinwisdomlights
version: 1.0.0
---

# 모노레포 패키지 관리 베스트 프랙티스


![스크린샷_2025-11-02_오후_3.14.11.png](https://res.cloudinary.com/dyrdul1dd/image/upload/v1768758494/norkive-notion-images/png_1768758491861_RTFfODRfOD.webp?format=webp&quality=85)


![스크린샷_2025-10-20_오전_11.26.25.png](https://res.cloudinary.com/dyrdul1dd/image/upload/v1768758500/norkive-notion-images/png_1768758498275_RTFfODRfOD.webp?format=webp&quality=85)


## 📊 실제 크기 분석


### 패키지 소스 코드 크기


일반적으로 npm 패키지의 소스 코드는 매우 작습니다:


```plain text
packages/youtube-utils/
├── src/index.ts          ~2-3 KB (소스 코드)
├── package.json          ~1 KB
├── tsconfig.json         ~0.5 KB
├── README.md             ~3-5 KB
└── .npmignore            ~0.2 KB

총 소스 코드: ~7-10 KB (매우 작음!)
```


### 빌드 산출물 (제외해야 함)


```plain text
packages/youtube-utils/dist/
├── index.js              ~2 KB (CommonJS)
├── index.mjs             ~1 KB (ESM)
└── index.d.ts            ~1 KB (타입 정의)

총 빌드 산출물: ~4 KB (Git에 포함 불필요)
```


### node_modules (절대 포함하지 않음)


```plain text
packages/youtube-utils/node_modules/  ~50-100 MB (큼!)
```


## ✅ 권장 사항: 모노레포 패턴


### 일반적인 모노레포 프로젝트 구조


대부분의 오픈소스 프로젝트들도 이렇게 합니다:

1. <strong>소스 코드 포함</strong> ✅
    - <code>packages/*/src/</code> - 소스 코드 (작고 필요)
    - <code>packages/*/package.json</code> - 패키지 설정
    - <code>packages/*/tsconfig.json</code> - TypeScript 설정
    - <code>packages/*/README.md</code> - 문서
2. <strong>빌드 산출물 제외</strong> ❌
    - <code>packages/*/dist/</code> - 빌드 결과물 (자동 생성)
    - <code>.next/</code> - Next.js 빌드 결과물과 동일한 이유
3. <strong>의존성 제외</strong> ❌
    - <code>packages/*/node_modules/</code> - 각 패키지의 의존성
    - 루트 <code>node_modules/</code>와 동일한 이유

## 📁 .gitignore 권장 설정


```plain text
# 패키지 빌드 산출물 (자동 생성)
packages/*/dist/
packages/*/build/
packages/*/.turbo/

# 패키지 의존성
packages/*/node_modules/

# 패키지 로그 및 캐시
packages/*/npm-debug.log*
packages/*/yarn-debug.log*
packages/*/.npm/
packages/*/.cache/

# 패키지 테스트 결과
packages/*/coverage/
packages/*/.nyc_output/

# 패키지 임시 파일
packages/*/test.js
packages/*/*.test.ts
```


## 🔍 실제 프로젝트 사례


### 1. Turborepo (Vercel)

- 소스 코드: 포함 ✅
- 빌드 산출물: 제외 ❌
- node_modules: 제외 ❌

### 2. Nx (Nrwl)

- 소스 코드: 포함 ✅
- 빌드 산출물: 제외 ❌
- node_modules: 제외 ❌

### 3. Lerna 프로젝트들

- 소스 코드: 포함 ✅
- 빌드 산출물: 제외 ❌
- node_modules: 제외 ❌

## 💡 결론


### 걱정할 필요 없음! ✅

1. <strong>소스 코드는 매우 작음</strong>
    - 각 패키지: ~10 KB
    - 10개 패키지: ~100 KB (무시 가능한 크기)
2. <strong>빌드 산출물은 제외</strong>
    - <code>.gitignore</code>에 추가하면 됨
    - 필요시 <code>npm run build</code>로 재생성 가능
3. <strong>일반적인 패턴</strong>
    - 대부분의 모노레포 프로젝트가 이렇게 함
    - 소스 코드 포함, 빌드 산출물 제외

## 🎯 권장 작업

1. <code>.gitignore</code>에 패키지 관련 항목 추가
2. 소스 코드는 Git에 포함 (작고 필요)
3. 빌드 산출물은 제외 (자동 생성)
4. npm 배포 시 <code>dist/</code>만 포함 (<code>.npmignore</code> 사용)

## 📦 npm 배포 시


배포할 때는:

- <code>dist/</code> 폴더만 포함 (<code>.npmignore</code>로 설정)
- 소스 코드는 제외 (npm 사용자는 빌드된 코드만 필요)

로컬 개발 시:

- 소스 코드 포함 (개발 편의성)
- 빌드 산출물 제외 (Git 용량 절약)

## ✨ 최종 결론


<strong>걱정하지 마세요!</strong>

- 소스 코드 포함은 정상적인 모노레포 패턴입니다
- 실제 크기는 매우 작습니다 (~10 KB/패키지)
- 빌드 산출물만 제외하면 됩니다
- 대부분의 개발자들이 이렇게 합니다
